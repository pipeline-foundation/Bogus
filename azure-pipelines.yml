# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:

- task: SonarCloudPrepare@1
  displayName: SonarCloud prepare
  inputs:
    SonarCloud: 'Bogus-azdo'
    organization: 'szlatkow'
    scannerMode: 'MSBuild'
    projectKey: 'Bogus-azdo'
    projectName: 'Bogus-azdo'

- task: DotNetCoreCLI@2
  displayName: dotnet tool restore
  inputs:
    command: 'custom'
    custom: 'tool'
    arguments: 'restore'

- task: DotNetCoreCLI@2
  displayName: dotnet fake run
  inputs:
    command: 'custom'
    custom: 'fake'
    arguments: run Source\Builder\build.fsx target ci

- task: DotNetCoreCLI@2
  displayName: Run Tests
  inputs:
    command: 'test'
    projects: 'Source\Bogus.Tests'
  
- task: SonarCloudAnalyze@1
  displayName: SonarCloud Analyze
  
- task: SonarCloudPublish@1
  displayName: SonarCloud publish
  inputs:
    pollingTimeoutSec: '300'

- task: dependency-check-build-task@5
  displayName: Dependency check
  inputs:
    projectName: 'Bogus.csproj'
    scanPath: 'Source/Bogus/Bogus.csproj'
    format: 'HTML'
- task: UniversalPackages@0
  inputs:
    command: 'publish'
    publishDirectory: '$(Build.ArtifactStagingDirectory)'
    feedsToUsePublish: 'internal'
    vstsFeedPublish: '93c69577-de9d-4205-bcf5-d615d43cf61d'
    vstsFeedPackagePublish: 'BogusFeed'
    versionOption: 'custom'
    packagePublishDescription: 'package'
    publishedPackageVar: 'Zlatkow-Feed'
